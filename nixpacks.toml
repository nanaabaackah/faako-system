[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "corepack enable",
  "corepack prepare pnpm@10.33.0 --activate",
  "pnpm install --frozen-lockfile"
]

[phases.build]
cmds = ["node ./scripts/railway-service.mjs build"]

[start]
cmd = "node ./scripts/railway-service.mjs start"
