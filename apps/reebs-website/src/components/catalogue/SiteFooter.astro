---
import StorefrontFooterIsland from "../islands/StorefrontFooterIsland.jsx";

const { currentPath = Astro.url.pathname } = Astro.props;
---

<StorefrontFooterIsland path={currentPath} client:idle={{ timeout: 900 }} />
