Intentionally minimal static publish directory for the standalone Faako API Netlify site.

Actual request handling lives in `../functions`. Keep this folder small unless the API site needs a simple static response alongside the functions runtime.
