{
  "name": "faako-new",
  "private": true,
  "packageManager": "pnpm@10.32.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "dev:faako": "node ./scripts/dev-faako.mjs",
    "dev:reebs": "pnpm --filter @faako/reebs-website run dev:with-backend",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "dependencies": {
    "react-helmet": "^6.1.0"
  }
}
